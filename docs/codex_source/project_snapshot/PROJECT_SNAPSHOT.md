# PROJECT SNAPSHOT

STATUS: BOUNDED_CODEX_REPO_SNAPSHOT
SOURCE_KIND: current_dialogue_delta_checked_against_repo_docs
SNAPSHOT_ID: PROJECT_SNAPSHOT_20260618_YOUTUBE_EDITOR_QUEUE_TARGET_DIVERGENCE_FIVE_STEP_PROOF_REQUIRED
DATE_UTC: 2026-06-18

## Git state

* private source repo HEAD at docs snapshot: `02083c989c0707b1980e1e50a61a364cf337ac03`
* branch: `main`
* origin/main verification: recorded in docs-only run report
* working tree may contain unrelated pre-existing dirty files; they are outside docs scope and must not be staged
* docs snapshot scope: `docs/codex_source/**` only
* no application code, runtime state, token, secret, auth, Telegram, provider, model, or database content is included

## Current active block

`youtube_prepare_post_draft_action_target_divergence_five_step_proof`

## Current proven project state

* The YouTube workflow has advanced through search, selection, video moderation, post-draft preparation, draft moderation, and separate publication boundaries.
* `youtube.prepare_post_draft` remains a non-publication tool family.
* Deterministic business state owns current queue and lifecycle facts.
* User-facing proof must remain:
  Telegram operator imitation -> project handler -> selected agent -> normal tool/facade path -> deterministic business layer -> real Telegram delivery proof.
* Direct backend, direct service call, direct DB action, manual SQL, fake Telegram path, and model-only inference are not valid user-flow proof substitutes.

## Current failure

A fresh deterministic editor queue showed seven ready-for-creation candidates.

A later real editing action attempted an unrelated candidate not present in that fresh queue and received a finalized / unsuitable result.

The first target-ID divergence is not yet proven.

## Required next proof

The next technical run must trace:

* STEP -5
* STEP -4
* STEP -3
* STEP -2
* STEP -1
* FIRST BROKEN STEP

At every point it must record file/function, input values, output values, value source, next consumer, and fresh-queue invariant status.

No source fix may begin before that proof finds the earliest target substitution.

## Hard boundaries

* no publication;
* no queue advancement in the next proof;
* no draft creation;
* no draft moderation;
* no DB mutation;
* no direct Telegram API;
* no direct backend substitute;
* no runtime/profile modification;
* no session cleanup;
* no phrase-specific patch;
* no hardcoded candidate IDs, queue counts, user IDs, chat IDs, agent IDs, session IDs, message IDs, provider, or model.

## Historical memory

Detailed earlier project history remains in append-only context, roadmap, and module-map files. This snapshot is current-state only.
